# План на выезд: «Собраться на рыбалку»

Оценка клёва отвечает на вопрос «ехать ли». План отвечает на другой: рыболов уже
стоит перед водой, и ему нужно знать, **на какой глубине, чем, сколько и когда**.

Документ описывает, что раздел считает сейчас, где он врёт, и какой моделью это
заменяется. Разбор проведён по коду
[`FishingStrategy.kt`](../../../src/main/java/com/example/fishforecast/domain/session/FishingStrategy.kt)
(699 строк) и справочнику
[`initial_fish.json`](../../../src/main/assets/initial_fish.json).

---

## 1. Что происходит сейчас

Экран показывает 11 блоков. Вот чем каждый из них на самом деле управляется:

| Блок | Откуда берётся | Зависит от вида? |
|---|---|---|
| Насадка, запасная | `baits.cold` / `baits.warm` | **да** |
| Прикормка | `groundbait_rules.cold/warm` | **да** |
| Что искать на месте | `preferred_structures` | **да** |
| Куда идти | температуры слоёв и их оценки | почти нет |
| Горизонт | вода, кислород, фаза света | **нет** |
| Схема закорма | цель + холодно/тепло + размер водоёма | **нет** |
| Размер насадки | схема закорма | **нет** |
| Монтаж | способ ловли | **нет** |
| Окно | прогноз клёва | только через балл |
| Раскладка суток | гильдия | только хищник/мирная |
| Предупреждения | вода, кислород, способ | почти нет |

**Четыре блока из одиннадцати знают, за какой рыбой едет человек.** Остальные
семь выдают одно и то же карпу, налиму и щуке. Отсюда и ощущение, что «у всех
всё одинаковое»: оно не ощущение, а устройство.

### 1.1. Мёртвые данные

Два поля справочника заполнены по всем видам, доезжают до базы — и не участвуют
в плане. Это тот же дефект, что был со структурами и наблюдениями.

**`default_horizon`** — есть в JSON, есть в `FishEntity`, дважды перенесён
миграциями, показан во вкладке «Знания». План его **не читает**: `horizonFor()`
считает горизонт только по температуре, кислороду и свету. Результат:

| Вид | В справочнике | Что скажет план в обычной воде |
|---|---|---|
| Плотва | `midwater` | Дно |
| Щука | `midwater` | Дно |
| Белый амур | `surface` | Дно |

Амуру, которого ловят с поверхности на камыш и плавающую кукурузу, приложение
советует дно.

**`flavor_profile`** — по каждому виду и по каждой воде записано, чем должен
пахнуть стол: у карпа `spicy_salty` / `sweet_fruity`, у карася `garlic_hemp` /
`vanilla_honey`, у леща `coriander_spicy` / `caramel_vanilla`, у плотвы
`anis_hemp` / `chocolate_anis`. В план не попадает ничего: строка прикормки
говорит объём, фракцию и сладость, но не запах.

### 1.2. Общие пороги вместо своих

Выбор приманки хищнику идёт через глобальную константу `COLD_WATER_C = 12.0`,
хотя у каждого вида есть собственный порог: щука 10, окунь 11, судак 12. При
воде 11 ° щуке советуют «холодную» подачу, хотя для неё это ещё тёплая вода.

### 1.3. Сезона нет вообще

Поиск по всему коду и справочнику даёт **ноль** совпадений на «нерест», `spawn`,
`season`, «сезон». Вся ось времени — один булев признак:

```kotlin
val cold = water < fish.coldTempThreshold
```

Одно число, одна граница, никакого направления и никакого календаря. Из этого
следуют настоящие ошибки:

- **Налим.** В справочнике у него `baits.warm` = `["Не клюет (впадает в
  оцепенение при T > 15°C)"]`. Это предложение план выводит **как насадку**:
  «Насадка: Не клюет (впадает в оцепенение при T > 15°C)». Летом раздел
  предлагает ехать за рыбой, которая спит.
- **Белый амур** с порогом 18 при воде 17 ° получает совет «мотыль, опарыш,
  червь» — животные насадки для рыбы, которая ниже 15 ° практически не ест, а
  всерьёз кормится от 20 °.
- **Нерест не существует.** В мае карп на нересте и карп в преднерестовом жоре
  получают одинаковый план, хотя в первом случае ехать незачем, а во втором это
  лучшая неделя года.
- **Нет разницы между «весной 14 °» и «осенью 14 °»**, хотя рыба ведёт себя
  противоположно: весной поднимается и ест впрок, осенью нагуливает и скатывается.
  Приложение уже считает ход воды и акклимацию — направление известно, но план
  его не спрашивает.

### 1.4. Желание рыбы не доходит до снасти

Балл клёва считается по 11 факторам и попадает в план только двумя способами:
выбрать слой и выбрать окно. Объём корма от него не зависит вовсе. Получается,
что при балле 20 и при балле 85 приложение советует один и тот же стол.

---

## 2. Что говорят источники

### 2.1. Пороги по видам

Сведено из перечисленных ниже источников; температура воды, °C.

| Вид | Нерест | Кормится от | Оптимум | Замирает |
|---|---|---|---|---|
| Карп / сазан | 18–22 | 10 | 18–24 | <8, почти не ест <4 |
| Карась | 17–20 | 8 | 16–26 | <6 |
| Лещ | 12–20 | 8 | 14–22 | <5 |
| Плотва | 12–15 | 4 | 12–20 | кормится и зимой |
| Окунь | 12–14 | 4 | 12–20 | крупный редко ест <4 |
| Щука | 4–10 | 2 | 12–18 | ест круглый год |
| Судак | 10–14 | 4 | 14–22 | ест зимой |
| Белый амур | 20–24 | 7–8 слабо, всерьёз 20 | 22–28 | <12 почти не ест |
| Сом | 17–20 | 12 | 22–26 | <10 не ест |
| Налим | 1–4, подо льдом, XI–III | — | 8–13 | **>15 оцепенение** |

Границы «замирает» согласуются с общей шкалой: 6–7 ° — рыба ест умеренно, 4–5 °
— с перерывами, 2–3 ° — почти неподвижна.

### 2.2. Нерест — не точка, а три фазы

- **Преднерестовый жор.** Вода растёт и подходит к нерестовой полосе снизу.
- **Нерест.** Клёва нет, и во многих регионах действует запрет.
- **Восстановление: 7–14 суток**, тем короче, чем стабильнее и теплее вода.
  Затем — самая интенсивная кормёжка года.

Это даёт три разных плана там, где сейчас один.

### 2.3. Аппетит меняется плавно, а не ступенькой

Скорость опорожнения кишечника у карповых (лещ, густера, плотва, карась, карп)
описывается **экспонентой от температуры**. То есть объём корма должен расти
плавно вместе с водой, а не переключаться в одной точке `cold_temp_threshold`.

Практика ловли говорит то же: в холодной воде — горсть, мелкая фракция, низкая
жирность, привлечение без насыщения; перекорм в холоде прямо уменьшает число
поклёвок.

---

## 3. Что сделано

### 3.1. Фаза сезона вида

`seasonPhaseOf(fish, history, now)` в
[`domain/season/SeasonPhase.kt`](../../../src/main/java/com/example/fishforecast/domain/season/SeasonPhase.kt)
— чистая функция без Android, как и весь `domain`. Возвращает фазу и всё, чем
её объясняют: воду, ход за окно и сколько суток назад был нерест.

| Фаза | Когда | Что меняет |
|---|---|---|
| `DORMANT` | вода выше `dormant_above_c` | насадки нет вовсе, стол не нужен, предупреждение |
| `WINTER` | ниже `feed_start_c` | животное, по одной штуке |
| `PRE_SPAWN` | вода растёт и вошла в 3° под нерестовой полосой | полный стол, крупная насадка |
| `SPAWN` | в полосе нереста **на подъёме** | предупреждение о возможном запрете |
| `POST_SPAWN` | вышли из полосы **вверх**, не дальше `post_spawn_recovery_days` | мало и мелко |
| `SUMMER` | в оптимуме | обычный план |
| `HEAT` | выше `opt_max` | горизонт выше обычного, стол меньше |
| `AUTUMN` | вода падает, ниже `opt_max` | животное про запас, стол держать |

Направление — ход воды за трое суток, шум отсекается порогом 0.5 °C. Календаря
в расчёте нет вовсе: нерестовую полосу засчитываем только на подъёме, осенью
та же вода идёт вниз и это нагул. Нерестовый запрет региональный, поэтому фаза
о нём **предупреждает**, а не блокирует.

Чего модель не умеет и не притворяется, что умеет: истории семь суток, а
восстановление длится до четырнадцати. Если нерест прошёл раньше начала
истории, `daysSinceSpawn` возвращает пустоту, а не догадку, — и посленерестовый
жор, который литература называет лучшим временем года, приложение не различает.

### 3.2. Сезон в расчёте клёва

Фактор `Сезон` — **ограничитель**, множится со всем остальным, вес 0:

| Фаза | Множитель |
|---|---|
| Оцепенение | 0.15 |
| Нерест | 0.45 |
| После нереста | 0.65 |
| Зимнее стояние | 0.85 |
| Жара, лето | 1.00 |
| Осенний нагул | 1.15 |
| Преднерестовый жор | 1.20 |

Температуру он не повторяет — её наказывает свой фактор, и складывать было бы
двойным счётом. Сезон отвечает за другое: чем рыба занята помимо еды. Считается
один раз на весь прогноз, а не на каждый час: за вечер рыба не отнерестится.

### 3.3. Аппетит

`appetiteOf(fish, water, season, conditions)` → 0…1, произведение трёх вещей:
удалённости воды от оптимума (плавно, а не ступенькой), фазы сезона и
сегодняшнего балла клёва (мягко, не ниже 0.6 от собственного вклада). Из него
следуют объём стола, размер насадки и поправка к схеме закорма.

| Аппетит | Стол |
|---|---|
| < 15 % | почти не кормить |
| < 35 % | мало корма, насадка мельче схемы |
| < 65 % | умеренно, схему сыпать вполовину |
| иначе | по справочнику вида |

Вверх объём не растёт никогда: перекормить проще, чем недокормить, и цена
ошибки разная.

### 3.4. Оживлённые данные

- **`default_horizon`** стал основой горизонта. Вода, кислород и сумерки
  поднимают вид на ступень выше обычного для него, а не считают горизонт с
  нуля. Плотва получает полводы, амур — верх.
- **`flavor_profile`** вышел в строку прикормки. Заодно словарь ароматов
  дополнен с восьми значений до всех пятнадцати, что есть в справочнике: лещу
  и плотве выводилось `coriander spicy` и `chocolate anis` — ключ вместо слова.
- **`horizonText`** ждал `mid` и `top`, которых в справочнике нет: там
  `midwater` и `surface`. Плотва и щука показывали во вкладке «Знания»
  английское «midwater». Словарь переехал в `domain/fish/FishWords.kt`, чтобы
  справочник и план говорили одними словами.
- **Пороги приманки хищника** берутся из `cold_temp_threshold` вида вместо
  общей константы 12 °: щука догоняет приманку с десяти, судаку нужно
  двенадцать.

### 3.5. Согласованность советов

План собирается из отдельных функций, и они умели противоречить друг другу в
пределах одного экрана. Убрано:

- «Вода 15° **в пределах оптимума 20–27°**» — ветка `else` утверждала оптимум,
  не проверяя его;
- «Прикормка: **умеренно**… стол можно держать **полным**» — причина описывала
  фазу вообще, а не принятое решение;
- «Схема закорма: ковёр, **обильно**» рядом с «умеренно» — схема не знала об
  аппетите;
- «Насадка: кукуруза… на нагуле тянется к **животному**» — совет спорил сам с
  собой; теперь осенью животная насадка становится запасной.

### 3.6. Данные справочника

По каждому виду добавлен блок:

```json
"season": {
  "spawn_temp_min": 18.0, "spawn_temp_max": 22.0,
  "feed_start_c": 10.0, "post_spawn_recovery_days": 10
}
```

У налима вместо `feed_start_c` стоит `dormant_above_c: 15.0`, а строка-заглушка
«Не клюет (впадает в оцепенение при T > 15°C)» из `baits.warm` убрана: это не
насадка, а состояние, и говорит о нём фаза.

Схема базы — версия 25, миграция `24 → 25` добавляет пять столбцов.

## 4. Границы

- Календарь не решает — решает вода. Даты в расчёте нет вовсе.
- Фаза **входит** в балл клёва отдельным ограничителем, но не повторяет
  температуру: за неё отвечает свой фактор.
- Знание остаётся данными: пороги нереста и кормления едут в справочнике.
- Посленерестовый жор не различается: истории семь суток, восстановление до
  четырнадцати. Отказ честнее догадки.

## Источники

- [Specziár, In situ estimates of gut evacuation and its dependence on temperature in five cyprinids, Journal of Fish Biology 2002](https://onlinelibrary.wiley.com/doi/10.1111/j.1095-8649.2002.tb01716.x)
- [Angling Times, Understanding water temperature](https://www.anglingtimes.co.uk/advice/tips/understanding-water-temperature/)
- [Angling Trust, When & where do freshwater fish spawn](https://linesonthewater.anglingtrust.net/2021/04/27/when-where-do-freshwater-fish-spawn/)
- [Master Fishing Guide, Pike spawning 4–10 °C](https://masterfishingguide.com/pike-spawning-in-spring/)
- [Master Fishing Guide, Post-spawn pike](https://masterfishingguide.com/post-spawn-pike-in-spring/)
- [Ahrex, Post spawn pike — appetite for destruction](https://ahrexhooks.com/2018/04/26/post-spawn-pike-appetite-for-destruction/)
- [FishBase, Lota lota](https://www.fishbase.se/summary/lota-lota.html)
- [BC ACAT, Burbot ecology and life history](https://a100.gov.bc.ca/pub/acat/documents/r10789/8840209-App.A-Speciessummary-Burbot_1188324646146_f6d9ed881f674be38a50ea8d31322657.pdf)
- [USGS NAS, Grass carp species profile](https://nas.er.usgs.gov/queries/FactSheet.aspx?speciesID=514)
- [Kilambi & Robison, Effects of temperature and stocking density on food consumption and growth of grass carp, J. Fish Biology 1979](https://onlinelibrary.wiley.com/doi/10.1111/j.1095-8649.1979.tb03614.x)
- [Seriously Fish, Silurus glanis](https://www.seriouslyfish.com/species/silurus-glanis/)
- [USGS NAS, Zander species profile](https://nas.er.usgs.gov/queries/factsheet.aspx?SpeciesID=830)
- [MDB, Crucian carp](https://www.mdb.fish/fish-fact-sheets/crucian-carp)
- [DNA Baits, Winter carp fishing guide](https://dnabaits.com/winter-carp-fishing-guide-2/)
- [Angling Direct, Best winter carp bait](https://www.anglingdirect.co.uk/community/what-best-winter-carp-bait/)
