<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Только каталоги для обмена: наружу не должна утекать вся внутренняя память. -->
    <cache-path
        name="shared"
        path="shared/" />
    <!-- Снимки уловов: камера пишет прямо в файл приложения. -->
    <files-path
        name="catches"
        path="catches/" />
</paths>
